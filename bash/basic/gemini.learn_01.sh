#!/bin/bash
# --------------------------------------------------------------------------------
# gemini.learn.sh
# --------------------------------------------------------------------------------

# 1. 堅牢化のオプション設定
# set -e (errexit): コマンドが失敗した時点でスクリプトを即座に終了させる。
# set -u (nounset): 未定義の変数を使おうとした時にエラーを出す（タイポ防止）。
# set -o pipefail: パイプラインの途中でエラーが起きても、全体の終了ステータスを「失敗」として扱う。
set -euo pipefail

# 2. メイン処理（ここに実際のコマンドを書く）
echo "処理を開始します..."

# サンプル処理：存在しないコマンドを実行してみる（set -eの効果確認用）
# ls /non_existent_directory

echo "処理が正常に完了しました。"
