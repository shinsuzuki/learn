function CounterApp() {
    // // ①useStateを使ってカウントのステートを定義
    // const [count, setCount] = useState(0);

    // const handleClick = () => {
    //     // ②ボタンがクリックされたときにカウントを1増やす
    //     setCount(count + 1);
    // };
    
    // 変数に初期値をつめる
    let count = 0;

    const handleClick = () => {
        // ボタンがクリックされたときにカウントを1増やす
        count += 1;
        console.log(`現在のカウント: ${count}`);
        // 画面に反映するためには、useStateを使う必要がある
    };

    return (
・・・既存コード・・・
