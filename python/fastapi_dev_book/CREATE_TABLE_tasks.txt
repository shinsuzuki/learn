CREATE TABLE tasks (
        id INTEGER NOT NULL,
        title VARCHAR(1024),
        PRIMARY KEY (id)
);


CREATE TABLE dones (
        id INTEGER NOT NULL,
        PRIMARY KEY (id),
        FOREIGN KEY(id) REFERENCES tasks (id)
);